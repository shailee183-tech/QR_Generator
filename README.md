# QR_Generator
QR Generator is a Python-based application that creates QR codes from URLs and text using the "qrcode" and "Pillow" libraries. It supports both basic and customizable QR code generation with options for color, size, border, and error correction.
